import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'be_productive',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
